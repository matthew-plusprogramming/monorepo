# State Machines Template (State Diagram Source)
#
# Getting started:
#   1. Run `node .claude/scripts/docs-scaffold-diagrams.mjs` to generate a stub
#      with the schema_version header and minimal valid structure.
#   2. Or fill in this template manually with your project's state machines.
#   3. Run `node .claude/scripts/docs-validate.mjs` to check for issues.
#   4. Run `node .claude/scripts/docs-generate.mjs` to create Mermaid state diagrams.
#
# Each state machine defines states and transitions.
# The generator produces a stateDiagram-v2 from this source.
#
schema_version: 1
state_machines: []
